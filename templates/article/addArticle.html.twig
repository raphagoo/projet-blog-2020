{% extends 'base.html.twig' %}

{% block body %}


	<div class="shadow-lg p-3 mb-5 bg-white rounded w-75 p-3 mx-auto mt-5">
		<h1 class="text-center">Add article</h1>

		<div class="example-wrapper">
			{{ form_start(form) }}
			{{ form_widget(form) }}
			{{ form_end(form) }}
		</div>
	</div>
{% endblock %}
